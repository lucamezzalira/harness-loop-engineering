# Declared environment variables (keys only — no secrets).
# Must stay in sync with each service's config/env.js schema.

ORDERS_PORT=   # HTTP port for the orders service
BILLING_PORT=  # HTTP port for the billing service
LOG_LEVEL=     # info | warn | error
