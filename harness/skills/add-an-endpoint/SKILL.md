---
name: add-an-endpoint
description: Use when adding an HTTP route to a service.
---

1. Ask first if the route is public (boundaries).
2. Validate input at the edge; use `config/env.js` for config.
3. Add unit tests named in acceptance.json.
4. Run `./verify.sh --turn` before finishing.
