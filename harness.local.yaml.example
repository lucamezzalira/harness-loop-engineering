# Set exactly one tool. There is no default.
tool: cursor
profile: default
confirmProfile: true

# Hooks default on in harness.yaml. Override here only if you must disable.
# failClosed applies to beforeShell (and optional beforeMCP) when true.
hooks:
  enable: true
  failClosed: false
