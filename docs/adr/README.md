# Architecture Decision Records

Number ADRs as `NNNN-kebab-title.md` starting at `0001`.

Write one when a unit decides something that must outlive the unit:

- Service or package boundary
- Contract / event shape (additive vs breaking)
- Storage or identity strategy

Use the `write-an-adr` skill (copies `harness/templates/adr.md`).
