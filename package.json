{
  "name": "harness-loop-engineering",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Standalone harness for Node projects coding agents",
  "bin": {
    "harness-verify": "./verify.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify": "./verify.sh",
    "test": "node --test $(find harness/lib -name '*.test.mjs' | sort)",
    "adr:index": "node scripts/build-adr-index.mjs"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  }
}
